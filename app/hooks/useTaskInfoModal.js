import { create } from 'zustand';

const useTaskInfoModal = create((set) => ({
  isOpen: false,
  onOpen: () => set({ isOpen: true }),
  onClose: () => set({ isOpen: false }),
}));

export default useTaskInfoModal;
